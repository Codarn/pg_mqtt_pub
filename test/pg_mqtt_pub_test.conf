# PostgreSQL test configuration for pg_mqtt_pub
# This file is loaded during regression tests via REGRESS_OPTS

# Load the extension
shared_preload_libraries = 'pg_mqtt_pub'

# Basic test configuration
pg_mqtt_pub.broker_host = 'localhost'
pg_mqtt_pub.broker_port = 1883
pg_mqtt_pub.queue_size = 1024
